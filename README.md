# Elite

Monorepo for the Elite tuition platform.

- `client` — Next.js frontend
- `server` — Express API

## Client

```bash
cd client
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
# elit_tutor
